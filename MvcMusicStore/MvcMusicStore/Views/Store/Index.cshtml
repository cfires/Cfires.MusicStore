@model IEnumerable<Genre>
@{
    ViewBag.Title = "Index";
}

<h2>Browse Genres</h2>
<p>Select from @Model.Count() genres</p>
<ul>
    @foreach (var genre in Model)
    {
        <li><a href="@Url.Action("Browse", "Store", new { genre = genre.Name })">@genre.Name</a></li>
    }
</ul>
